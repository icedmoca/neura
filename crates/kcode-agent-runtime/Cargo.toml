[package]
name = "kcode-agent-runtime"
version = "0.1.0"
edition = "2024"

[lib]
name = "kcode_agent_runtime"
path = "src/lib.rs"

[dependencies]
thiserror = "1"
tokio = { version = "1", features = ["sync"] }
