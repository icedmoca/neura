[package]
name = "kcode-provider-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "kcode_provider_core"
path = "src/lib.rs"

[dependencies]
reqwest = { version = "0.12", features = ["json", "stream", "blocking"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
