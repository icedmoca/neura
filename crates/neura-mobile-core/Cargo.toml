[package]
name = "neura-mobile-core"
version = "0.1.0"
edition = "2024"
description = "Shared headless mobile simulator core for neura"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
